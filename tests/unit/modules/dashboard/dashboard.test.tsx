import { describe } from "vitest"

describe("Tests in <Dashboard.tsx />", () => {
	test("Should add a new search", () => {})

	test("Should not add a new search if this exists in the array", () => {})
})
